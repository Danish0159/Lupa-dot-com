import { createTheme } from "@mui/material";

// Material UI
//Global Styles.
export const theme = createTheme({
  typography: {
    fontFamily: ["Font"],
  },
});

// Material UI
export const styles = {
  textField: {
    fontSize: "1.3rem",
    fontWeight: "400",
    padding: "12px 14px",
    color: "#2a2a2a",
    lineHeight: "1.4",
  },
};
